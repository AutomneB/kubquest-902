apiVersion: v2
name: stack-902
version: 0.1.0
dependencies:
  - name: ingress-nginx
    version: "4.12.3"                      # Exemple de version récente stable, à ajuster
    repository: "https://kubernetes.github.io/ingress-nginx"
