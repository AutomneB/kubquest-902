entryPoints:
  metrics:
    address: ":9100"

metrics:
  prometheus:
    enabled: true
    entryPoint: metrics

service:
  ports:
    web:
      port: 80
      targetPort: web
    websecure:
      port: 443
      targetPort: websecure
    metrics:
      port: 9100
      targetPort: metrics
