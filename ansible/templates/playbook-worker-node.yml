### 📘 Playbook Ansible : Kubernetes Worker Node Setup (worker-playbook.yml)

- name: Join Kubernetes Cluster from Worker
  hosts: worker*
  become: true
  vars:
    kubeadm_join_cmd: "kubeadm join 10.0.1.4:6443 --token y2c7jy.45eu38dh3sit9ppv --discovery-token-ca-cert-hash sha256:269ec9112360af4eba6728a4bf8368f6b21f4fcb62a19fa639bc859b3ddfb2a9"

  tasks:
    - name: Joindre le cluster Kubernetes
      command: "{{ kubeadm_join_cmd }}"
      args:
        creates: /etc/kubernetes/kubelet.conf
