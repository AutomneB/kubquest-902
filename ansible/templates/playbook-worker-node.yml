### 📘 Playbook Ansible : Kubernetes Worker Node Setup (worker-playbook.yml)

- name: Join Kubernetes Cluster from Worker
  hosts: worker*
  become: true
  vars:
    kubeadm_join_cmd: "kubeadm join 10.0.1.4:6443 --token psz8av.lk9e7r7sra08w7v5 --discovery-token-ca-cert-hash sha256:886480ea6c2f7b55be2667c61373f5263875641e4c963427ecb75e44e9e3c57a"

  tasks:
    - name: Joindre le cluster Kubernetes
      command: "{{ kubeadm_join_cmd }}"
      args:
        creates: /etc/kubernetes/kubelet.conf
