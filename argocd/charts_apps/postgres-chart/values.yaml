postgresql:
  fullnameOverride: my-postgresql
  auth:
    username: myuser
    password: mypassword
    database: mydatabase
  primary:
    persistence:
      enabled: true
      size: 8Gi
  service:
    type: ClusterIP
